extends layout

block content
  h1= title
  p Welcome to #{title}

  form
   input(type="text", placeholder="country 1")
   input(type="text", placeholder="country 2")
   input(type="submit", value="create")